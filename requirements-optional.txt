# ZPlayer 可选依赖 — Whisper 语音识别功能
# 安装方式: pip install -r requirements-optional.txt
#
# 注意: torch + whisper 体积约 2GB+，仅在需要无内置字幕视频的
# 语音识别功能时安装。不安装也不影响 ZPlayer 其他功能正常使用。

openai-whisper>=20231117
torch>=2.0.0
